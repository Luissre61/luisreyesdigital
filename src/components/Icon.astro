---
interface Props {
  name: string;
  class?: string;
  width?: number;
  height?: number;
}

const { name, class: className, width = 24, height = 24 } = Astro.props;
---
<svg class:list={[className]} {width} {height} fill="currentColor">
  <use href={`/iconos/sprite.svg#${name}`} />
</svg>