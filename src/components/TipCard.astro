---
// Define las propiedades que el componente espera recibir
export interface Props {
  title: string;
  description: string; // Se añade la descripción como prop requerida
  icon: string;
}

// Extrae las propiedades del objeto Astro.props
const { icon, title, description } = Astro.props;
---

<!-- 
  Contenedor principal de la tarjeta.
  - Se añade la clase `card-item` para que el script `estilos.js` la encuentre.
  - Se unifican los estilos para que coincidan con el script y el diseño deseado.
-->
<div class="card-item flex flex-col bg-white p-6 rounded-2xl shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-2 cursor-pointer h-full border border-gray-200">
    <div class="flex items-center mb-4">
        <span class="text-3xl mr-4">{icon}</span>
        <h2 class="text-xl font-bold text-gray-800">{title}</h2>
    </div>
    <p class="text-gray-600 text-base leading-relaxed">
        {description}
    </p>
  </p>
</div>